# Example environment variables for Next.js
# Copy this file to .env.local and fill in your values. .env.local is ignored by git.

# Public variables (exposed to browser) must be prefixed with NEXT_PUBLIC_
NEXT_PUBLIC_SITE_NAME=TerraTrack

# Server-side only secrets (do NOT prefix with NEXT_PUBLIC_)
# API_BASE_URL=https://api.example.com
# API_KEY=your_api_key_here
